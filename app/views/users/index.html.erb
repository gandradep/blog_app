<div class="flex w80 mAuto buttonWrapper">
  <%= button_to 'Sign out', destroy_user_session_path, method: :delete, data: { turbo: false },
 class: 'button border'%>
</div>
<% @users.each do |person| %>
  <div class="wrapper flex w80 mAuto">
    <div>
      <%= image_tag(person.photo, :class => 'photo border')%>
    </div>
    <%= link_to user_path(:id => person.id), class: 'anchor_user' do%>
    <div class="user_info w100 border flex">
      <h2 class="w50"><%= person.name%></h2>
      <p class="w50 postNum">Number of posts: <%= person.postsCounter%></p>
    </div>
    <% end %>
  </div>
<% end %>
